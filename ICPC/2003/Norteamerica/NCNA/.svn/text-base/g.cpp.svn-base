#include <cstdio>
#include <cmath>

using namespace std;

int main(){
  int n, ncase=1;
  while(scanf("%d",&n)!=EOF && n){
    printf("Case %d, N = %d, # of different labelings = %d\n",ncase++,n,(int)pow(n,n-2));
  }
  return 0;
}
